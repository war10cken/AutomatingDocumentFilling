<UserControl x:Class="AutomatingDocumentFilling.WPF.Views.FirstPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AutomatingDocumentFilling.WPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" Content="Open document" Width="150" Command="{Binding OpenDocumentCommand}"/>
        <Button Grid.Row="3" Command="{Binding ShowWindowCommand}" Width="150" Content="Show"/>
        <!-- <DocumentViewer Grid.Row="1" Document="{Binding Document}"/> -->
        <TextBox Grid.Row="2" Text="{Binding Text, Mode=TwoWay}" Width="150" Margin="0 10 0 0"/>
        <!-- <FlowDocumentScrollViewer Grid.Row="1" Document="{Binding FlowDocument}" /> -->
    </Grid>
</UserControl>
